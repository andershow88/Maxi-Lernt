# Datenbank (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/maxi_lernt

# OpenAI API Key (optional — App funktioniert auch ohne)
OPENAI_API_KEY=

# Push-Benachrichtigungen (optional)
# Generieren mit: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:maxi.schoppe@gmail.com
